---
- name: Install tmux
  ansible.builtin.apt:
    name: tmux
    state: present
    update_cache: true

- name: Copy tmux configuration
  ansible.builtin.copy:
    src: tmux.conf
    dest: /etc/tmux.conf
    owner: root
    group: root
    mode: '0644'
